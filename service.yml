apiVersion: v1
kind: Service
metadata:
  name: my-app-service
spec:
  type: ClusterIP   # Change to NodePort or LoadBalancer if needed
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
